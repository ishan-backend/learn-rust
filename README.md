# learn-rust

##### main
    - fn name(<args>) {<body>}
    - macro is with !
    - mutable - mut -> value can be changed, else it cannot be changed
    - string formatting
    - error handling in rust